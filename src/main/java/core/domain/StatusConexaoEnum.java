package core.domain;

/**
 * Representa o estado de um pedido de conexão (estilo "rede social")
 * entre dois usuários do sistema.
 */
public enum StatusConexaoEnum {
    PENDENTE,
    ACEITA,
    RECUSADA
}
